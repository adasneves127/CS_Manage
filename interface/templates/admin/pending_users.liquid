<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Pending Users</title>
</head>
<body>
    <table>
        <thead>
            <tr>
                <th>Seq</th>
                <th>Name</th>
                <th>Date/Time Submitted</th>
                <th>Email</th>
                <th>Review</th>
            </tr>
        </thead>
        {% for user in pending %}
            <tr>
                <td>{{user[0]}}</td>
                <td>{{user[1]}}</td>
                <td>{{user[2]}}</td>
                <td>{{user[3]}}</td>
                <td><a class="nav" href="/auth/pending/review/{{user[0]}}">Open</a></td>
            </tr>
        {% endfor %}
    </table>
</body>
</html>